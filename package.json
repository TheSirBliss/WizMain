{
  "name": "wizai-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:api": "npm run dev --workspace=core-api"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": "18.x"
  }
}
