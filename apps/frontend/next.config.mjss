/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disabilita la generazione di source maps in produzione per risparmiare memoria
  productionBrowserSourceMaps: false,

  // Mantiene la configurazione precedente per ridurre l'uso della CPU
  experimental: {
    workerThreads: false,
    cpus: 1
  },
};

export default nextConfig;